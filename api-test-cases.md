# API Test Cases

## POST /v1/kkt/user/reg

| ID | Название | Эндпоинт | Предусловия | Шаги | Тестовые данные | Ожидаемый результат | Тип |
|---|---|---|---|---|---|---|---|
| API-POST-001 | Успешная регистрация пользователя ККТ с валидным userInn | /v1/kkt/user/reg | Есть валидный токен партнёра и валидное тестовое значение userInn | 1. Отправить POST-запрос на /v1/kkt/user/reg. 2. Передать валидный Partner JWT в заголовке Authorization. 3. Передать валидное значение userInn в теле запроса. | Валидный Partner JWT; валидное значение userInn | HTTP 200 OK. Тело ответа соответствует схеме RegisterInnResponse. | Позитивный |
| API-POST-002 | Проверка структуры и содержимого ответа при успешной регистрации | /v1/kkt/user/reg | Регистрация пользователя выполняется успешно | 1. Отправить корректный запрос на регистрацию. 2. Открыть тело ответа. 3. Проверить объект jwtPair. 4. При наличии его полей проверить их типы и форматы согласно схеме JwtPair. | Валидный токен партнёра; корректный userInn | HTTP 200 OK. При наличии полей jwtPair их типы и форматы соответствуют схеме JwtPair. | Позитивный |
| API-POST-003 | Отправка запроса без заголовка Authorization | /v1/kkt/user/reg | Есть корректный тестовый ИНН | 1. Открыть POST-запрос.<br>2. Не передавать Authorization.<br>3. Передать Body.<br>4. Отправить запрос. | Body: {"userInn": "<валидный ИНН>"} | Сервер отклоняет запрос и возвращает ошибку авторизации. Точный HTTP-код спецификацией не определён. | Негативный |
| API-POST-004 | Отправка запроса с невалидным токеном | /v1/kkt/user/reg | Есть корректный тестовый ИНН | 1. Передать некорректный токен.<br>2. Передать Body.<br>3. Отправить запрос. | Authorization: Bearer invalid_token; Body с userInn | Сервер отклоняет запрос и возвращает ошибку авторизации. | Негативный |
| API-POST-005 | Отправка запроса с просроченным токеном | /v1/kkt/user/reg | Есть просроченный токен партнёра | 1. Передать просроченный токен.<br>2. Передать корректный Body.<br>3. Отправить запрос. | Authorization: Bearer <просроченный токен> | Сервер отклоняет запрос с просроченным токеном. | Негативный |
| API-POST-006 | Передача токена пользователя ККТ вместо токена партнёра | /v1/kkt/user/reg | Есть валидный токен пользователя ККТ (KktUserJwtBearer) | 1. Передать токен пользователя ККТ.<br>2. Передать корректный Body.<br>3. Отправить запрос. | Authorization: Bearer <токен пользователя ККТ> | Сервер отклоняет запрос, так как используется неподходящий тип токена. | Негативный |
| API-POST-007 | Отправка запроса с пустым Body | /v1/kkt/user/reg | Есть валидный токен партнёра | 1. Передать валидный токен.<br>2. Не передавать Body (Content-Length отсутствует / Body полностью пустой).<br>3. Отправить запрос. | Body отсутствует | Сервер отклоняет запрос и возвращает ошибку данных запроса. | Негативный |
| API-POST-010 | Передача userInn с неправильным типом данных | /v1/kkt/user/reg | Есть валидный токен партнёра | 1. Передать валидный токен.<br>2. Передать число вместо строки.<br>3. Отправить запрос. | Body: {"userInn": 1234567890} | Переданное значение не соответствует типу string, указанному для userInn в схеме. Точный HTTP-код спецификацией не определён. | Негативный |
| API-POST-012 | Отправка невалидного (битого) JSON в теле | /v1/kkt/user/reg | Есть валидный токен партнёра | 1. Передать валидный токен.<br>2. Передать в Body синтаксически некорректный JSON (например, без закрывающей скобки).<br>3. Отправить запрос. | Body: {"userInn": "1234567890" (без закрывающей скобки) | Сервер не должен успешно обработать синтаксически некорректный JSON. Конкретный HTTP-код спецификацией не определён. | Негативный |
| API-POST-013 | Инъекция в поле userInn | /v1/kkt/user/reg | Есть валидный токен партнёра | 1. Передать валидный токен.<br>2. В Body передать userInn с SQL/NoSQL/скрипт-пейлоадом.<br>3. Отправить запрос. | Body: {"userInn": "' OR 1=1 --"} или {"userInn": "<script>alert(1)</script>"} | Переданное значение не должно приводить к выполнению инъекции или необработанной серверной ошибке. Правила валидации содержимого userInn в спецификации отсутствуют. | Негативный |
| API-POST-014 | Отправка запроса неверным HTTP-методом | /v1/kkt/user/reg | Есть валидный токен партнёра | 1.Отправить GET-запрос на /v1/kkt/user/reg вместо POST. | Метод = GET | Неподдерживаемый HTTP-метод не должен выполнять операцию регистрации. Конкретный HTTP-код спецификацией не определён. | Негативный |
| API-POST-015 | Отправка запроса с Content-Type, отличным от application/json | /v1/kkt/user/reg | Есть валидный токен партнёра | 1.Отправить POST-запрос на /v1/kkt/user/reg.<br>2.Передать валидный Partner JWT.<br>3.Указать Content-Type: text/plain.<br>4.Передать тело запроса. | Content-Type: text/plain; Body с userInn | Проверить обработку запроса с Content-Type, отличным от application/json. Поддержка такого Content-Type и конкретный HTTP-код спецификацией не определены. | Негативный |

## GET /v1/kkt/user/kkt/{serial_number}

| ID | Название | Эндпоинт | Предусловия | Шаги | Тестовые данные | Ожидаемый результат | Тип |
|---|---|---|---|---|---|---|---|
| API-GET-001 | Получение информации о существующей ККТ с валидным токеном | /v1/kkt/user/kkt/{serial_number} | Есть валидный токен пользователя ККТ и существующий заводской номер ККТ | 1. Открыть GET-запрос.<br>2. Подставить номер ККТ в URL.<br>3. Передать Bearer Token.<br>4. Отправить запрос. | URL: /v1/kkt/user/kkt/<существующий номер>; валидный токен | Сервер возвращает HTTP 200 OK. Тело ответа соответствует схеме GetKktResponse. | Позитивный |
| API-GET-002 | Проверка структуры объекта kkt | /v1/kkt/user/kkt/{serial_number} | Запрос возвращает 200 OK | 1. Отправить корректный GET-запрос. 2. Открыть тело ответа. 3. Проверить типы и форматы присутствующих полей объекта kkt согласно схеме KktInfo. | Существующий serial_number; валидный токен | HTTP 200 OK. Присутствующие поля объекта kkt имеют типы и форматы, соответствующие схеме KktInfo. | Позитивный |
| API-GET-003 | Проверка структуры объекта firmwareInfo | /v1/kkt/user/kkt/{serial_number} | Запрос возвращает данные о ККТ | 1. Отправить корректный GET-запрос. 2. Проверить объект firmwareInfo при его наличии. 3. Проверить типы присутствующих полей name и version | Существующий serial_number; валидный токен | При наличии firmwareInfo объект соответствует схеме FirmwareInfo. Присутствующие поля name и version имеют тип string. | Позитивный |
| API-GET-004 | Проверка соответствия serialNumber переданному номеру | /v1/kkt/user/kkt/{serial_number} | Есть существующая ККТ | 1. Передать номер ККТ в URL.<br>2. Отправить запрос.<br>3. Сравнить номер в URL со значением kkt.serialNumber. | URL содержит <номер ККТ> | При наличии поля kkt.serialNumber его значение соответствует номеру ККТ, переданному в запросе. | Позитивный |
| API-GET-005 | Проверка формата fnActiveTill | /v1/kkt/user/kkt/{serial_number} | Запрос возвращает данные о ККТ | 1. Отправить корректный запрос.<br>2. Открыть объект kkt.<br>3. Найти fnActiveTill.<br>4. Проверить формат значения. | Существующий serial_number; валидный токен | При наличии поля fnActiveTill его значение соответствует формату date-time. | Позитивный |
| API-GET-006 | Отправка запроса без Authorization | /v1/kkt/user/kkt/{serial_number} | Есть существующий номер ККТ | 1. Подставить номер ККТ.<br>2. Не передавать Authorization.<br>3. Отправить запрос. | URL с существующим номером; заголовок отсутствует | Сервер отклоняет запрос и возвращает ошибку авторизации  | Негативный |
| API-GET-007 | Отправка запроса с невалидным токеном | /v1/kkt/user/kkt/{serial_number} | Есть существующий номер ККТ | 1. Подставить номер ККТ.<br>2. Передать неправильный токен.<br>3. Отправить запрос. | Authorization: Bearer invalid_token | Сервер отклоняет запрос и возвращает ошибку авторизации  | Негативный |
| API-GET-008 | Отправка запроса с просроченным токеном | /v1/kkt/user/kkt/{serial_number} | Есть просроченный токен пользователя ККТ | 1. Подставить номер ККТ.<br>2. Передать просроченный токен.<br>3. Отправить запрос. | Authorization: Bearer <просроченный токен> | Сервер отклоняет запрос с просроченным токеном  | Негативный |
| API-GET-009 | Запрос ККТ с использованием токена другого пользователя | /v1/kkt/user/kkt/{serial_number} | Есть токен пользователя А и номер ККТ, зарегистрированный за пользователем Б | 1. Передать токен пользователя А.<br>2. Указать номер ККТ пользователя Б.<br>3. Отправить запрос. | Токен пользователя А; serial_number пользователя Б | Сервер возвращает ошибку доступа и не отдаёт данные о ККТ пользователя. | Негативный |
| API-GET-010 | Запрос с несуществующим номером ККТ | /v1/kkt/user/kkt/{serial_number} | Есть валидный токен | 1. Подставить номер, которого нет в системе.<br>2. Отправить запрос. | serial_number = <несуществующий номер> | Проверить обработку запроса с несуществующим serial_number. Ожидаемое поведение и конкретный HTTP-код спецификацией не определены. При возврате ошибки тело ответа соответствует схеме Status. | Негативный |
| API-GET-011 | Запрос с пустым значением serial_number | /v1/kkt/user/kkt/ | Есть валидный токен | 1. Удалить значение serial_number из URL.<br>2. Отправить запрос. | URL без номера ККТ | Запрос не приводит к успешному получению данных ККТ. Конкретный HTTP-код для запроса без значения serial_number спецификацией не определён. | Негативный |
| API-GET-014 | Проверка serial_number на инъекционные значения | /v1/kkt/user/kkt/{serial_number} | Есть валидный токен пользователя ККТ | 1.Подставить в serial_number SQL/скрипт-пейлоад.<br>2.Передать валидный токен.<br>3.Отправить запрос. | serial_number = ' OR 1=1 -- или <script>alert(1)</script> | Переданное значение не должно приводить к выполнению инъекции или необработанной серверной ошибке. Ограничения формата serial_number в спецификации отсутствуют. | Негативный |
| API-GET-015 | Отправка запроса неверным HTTP-методом | /v1/kkt/user/kkt/{serial_number} | Есть валидный токен пользователя ККТ и существующий serial_number | 1. Отправить POST/PUT/DELETE-запрос на этот путь вместо GET. | Метод = POST/PUT/DELETE | Неподдерживаемый HTTP-метод не должен выполнять операцию получения данных ККТ. Конкретный HTTP-код спецификацией не определён. | Негативный |

